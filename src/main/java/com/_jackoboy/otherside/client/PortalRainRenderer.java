package com._jackoboy.otherside.client;

/**
 * Portal rain renderer — DISABLED pending full hybrid weather system rewrite.
 * The end-portal shader approach creates visual artifacts (spikes).
 * Will be replaced by the hybrid vanilla weather + localized portal rain system.
 */
public class PortalRainRenderer {
    // Intentionally empty — rain rendering disabled until hybrid system is built
}
